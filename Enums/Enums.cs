namespace Gold_e_Shop.Enums
{
    public class Enums
    {
        public enum OrderStatus { pending, completed, cancelled }
        public enum MediaType { image, video }
        public enum UserRole { admin, user }
    }
}
